---
# deploy.yml

- name: Deploy Configurations to CloudVision
  hosts: FABRIC
  connection: local
  gather_facts: false
  tasks:
    - name: Deploy Configurations to CloudVision
      ansible.builtin.import_role:
        name: arista.avd.cv_deploy
